<h1 class="text-center"><%= t('reviews.new.title') %></h1>
<hr><br>

<%= render partial: 'form', locals: { review: @review, submit_text: t('reviews.new.submit'), last_user_id: @last_user_id } %>

<%= link_to 'Back', reviews_path, class: 'btn btn-outline-secondary btn-sm' %>
